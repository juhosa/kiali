{
  "name": "kiali",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest --coverage --verbose=true --silent=false",
    "format": "prettier --write",
    "prepare": "npm run build",
    "build": "microbundle -i src/index.js -o dist"
  },
  "dependencies": {
    "@babel/core": "^7.4.4",
    "@babel/plugin-proposal-class-properties": "^7.4.4",
    "@babel/preset-env": "^7.4.4"
  },
  "devDependencies": {
    "jest": "^24.8.0",
    "microbundle": "^0.11.0"
  }
}
